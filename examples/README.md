> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md in this repository for full context.

# Examples — Working Code for Each Governance Layer

These are actual source files from the convergence engine
demonstrating each governance layer in working Rust code.
All code compiles and passes tests.

## Layer 1: C2R — The Boundary

[layer1_c2r/irs.rs](layer1_c2r/irs.rs)

Look at: `TranslationRequest`, `CoherenceIntent`, `OperatorConstraints`,
and `irs_descending()`. This is the mechanical boundary where human
intent becomes algebraic constraints. Deterministic. No interpretation.

Key function: `irs_descending()` takes a `TranslationRequest`
(human language) and returns `OperatorConstraints` (algebra).

## Layer 2: IRS — The Bridge

[layer2_irs/irs.rs](layer2_irs/irs.rs)

Look at: `CoherenceSignature`, `IrsAscending`, `irs_ascending()`,
and `irs_descending_superposed()`. This is the bidirectional
bridge. Ascending reads algebraic state as semantic summary.
Descending translates intent to constraints. Superposed
descending blends multiple intents simultaneously.

Key function: `irs_ascending()` takes engine state and produces
a human-readable summary with exact rational values.

Key function: `irs_descending_superposed()` takes multiple
weighted intents and produces a blended velocity in exact
arithmetic.

## Layer 3: MDC — The Governance

[layer3_mdc/mdc.rs](layer3_mdc/mdc.rs)

Look at: `EchelonWeights`, `physics_echelon()`, `legal_echelon()`,
`strategic_echelon()`, `security_echelon()`, and `mdc_resolve()`.
Four independent advisors. Exact rational weights summing to 1.
Entropy measuring disagreement. Stasis when confidence is low.

Key function: `mdc_resolve()` takes four echelon hints and
produces a combined result with entropy and stasis detection.

Also see: `evaluate_upgrade()` — the MDC applied to code
changes, not just algebraic operators. Demonstrates the
governance layer is general-purpose.

## Layer 4: Chronometry — The Thread

[layer4_chronometry/](layer4_chronometry/)

- `ledger.rs` — the receipt. 64-byte canonical preimage.
  BLAKE3 hashed. Five invariants before write.
- `temporal_ledger.rs` — the consensus. Levels by K-index.
  Witness attestation. Merkle root. Finality threshold.
- `store.rs` — the log. Append-only binary. receipts.bin.
- `block.rs` — Merkle tree computation.

Key function: `canonical_preimage()` builds the exact 64
bytes that get hashed. Every field big-endian. No alternatives.
This is the atomic unit of provable time.

## Supporting Files

- `engine.rs` — the core algebra (Delta, Q, coherence_functional)
- `consensus.rs` — mesh-level sigma aggregation

These provide context for how the governance layers connect
to the convergence engine's mathematical core.

## Note on Biological Naming

These source files are from the original research codebase
and contain biological naming conventions (Genome, Species,
witness, elder). See the SAIOS-research repository DISCLAIMER
for the terminology mapping. The math is identical regardless
of the naming.
